public class Building extends Terrain {
    public Building(){
        hue = (float) 0.1;
        sat = (float) 1;
        elevation = (float) 0.2; // Fixed. Does not differentiate in elevation. 
    } 
}
